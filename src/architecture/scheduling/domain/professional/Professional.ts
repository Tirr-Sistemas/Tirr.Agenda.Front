/** Minimal professional projection required to choose an appointment slot. */
export interface SchedulingProfessional { readonly professionalId: string; readonly displayName: string; readonly durationInMinutes: number; readonly price: number; }
